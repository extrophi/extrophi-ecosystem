<<<<<<< HEAD
name: CI
=======
name: CI - Unified Scraper
>>>>>>> iac032/main

on:
  push:
    branches:
      - main
      - 'claude/**'  # Run CI on all claude/* branches
  pull_request:
    branches: [ main ]

env:
<<<<<<< HEAD
  CARGO_TERM_COLOR: always

jobs:
  rust-check:
    name: Rust Check & Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest]  # macOS only for now

    steps:
    - uses: actions/checkout@v4

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Cache Rust dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/bin/
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          src-tauri/target/
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

    - name: Install whisper-cpp (macOS)
      if: runner.os == 'macOS'
      run: brew install whisper-cpp

    - name: Install whisper-cpp (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential pkg-config libglib2.0-dev libgtk-3-dev libwebkit2gtk-4.1-dev libsoup-3.0-dev libjavascriptcoregtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev libasound2-dev

    - name: Cargo check
      working-directory: src-tauri
      run: |
        if [ "${{ runner.os }}" == "Linux" ]; then
          cargo check --no-default-features
        else
          cargo check --all-features
        fi

    - name: Cargo test
      working-directory: src-tauri
      run: |
        if [ "${{ runner.os }}" == "Linux" ]; then
          cargo test --no-default-features --lib --bins
        else
          cargo test --all-features --lib --bins  # Skip doc tests for now
        fi

    - name: Cargo clippy
      working-directory: src-tauri
      run: |
        if [ "${{ runner.os }}" == "Linux" ]; then
          cargo clippy --no-default-features -- -D warnings || true
        else
          cargo clippy --all-features -- -D warnings || true
        fi

  frontend-check:
    name: Frontend Check & Test
=======
  PYTHON_VERSION: '3.11'

jobs:
  python-lint:
    name: Python Lint & Type Check
>>>>>>> iac032/main
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

<<<<<<< HEAD
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Svelte type check
      run: npm run check || true  # Allow type warnings for now

    - name: Run tests
      run: npm test

    - name: Lint
      run: npm run lint

  build-verification:
    name: Build Verification
    runs-on: ${{ matrix.os }}
    needs: [rust-check, frontend-check]
    strategy:
      matrix:
        os: [macos-latest]  # macOS only for now
=======
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'

    - name: Install linting tools
      run: |
        pip install ruff mypy black isort

    - name: Check formatting (Black)
      run: black --check backend/

    - name: Check import sorting (isort)
      run: isort --check-only backend/

    - name: Lint (Ruff)
      run: ruff check backend/

    - name: Type check (MyPy)
      run: mypy --ignore-missing-imports backend/

  python-test:
    name: Python Unit Tests
    runs-on: ubuntu-latest
    needs: python-lint

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_pass
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
>>>>>>> iac032/main

    steps:
    - uses: actions/checkout@v4

<<<<<<< HEAD
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install whisper-cpp (macOS)
      if: runner.os == 'macOS'
      run: brew install whisper-cpp

    - name: Install whisper-cpp (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential pkg-config libgtk-3-dev webkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev
        git clone https://github.com/ggerganov/whisper.cpp
        cd whisper.cpp
        make
        sudo make install
        sudo ldconfig

    - name: Install frontend dependencies
      run: npm ci

    - name: Build application
      run: npm run tauri:build

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: braindump-${{ runner.os }}
        path: |
          src-tauri/target/release/bundle/
        retention-days: 7
=======
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'

    - name: Install dependencies
      run: |
        pip install -r backend/requirements.txt
        pip install pytest pytest-asyncio pytest-cov

    - name: Run unit tests
      env:
        DATABASE_URL: postgresql://test_user:test_pass@localhost:5432/test_db
        REDIS_URL: redis://localhost:6379
      run: |
        pytest tests/unit/ -v --cov=backend --cov=scrapers --cov-report=xml

    - name: Upload coverage
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml

  build-containers:
    name: Build Podman Containers
    runs-on: ubuntu-latest
    needs: python-test

    steps:
    - uses: actions/checkout@v4

    - name: Setup Podman
      run: |
        sudo apt-get update
        sudo apt-get install -y podman

    - name: Build Core Engine
      run: |
        podman build -t unified_scraper_core ./backend
        echo "✅ Core engine built successfully"

    - name: Build Twitter Scraper Module
      run: |
        podman build -t unified_scraper_twitter ./scrapers/twitter
        echo "✅ Twitter scraper built successfully"

    - name: Build YouTube Scraper Module
      run: |
        podman build -t unified_scraper_youtube ./scrapers/youtube
        echo "✅ YouTube scraper built successfully"

    - name: Build Reddit Scraper Module
      run: |
        podman build -t unified_scraper_reddit ./scrapers/reddit
        echo "✅ Reddit scraper built successfully"

    - name: Build Web Scraper Module
      run: |
        podman build -t unified_scraper_web ./scrapers/web
        echo "✅ Web scraper built successfully"

  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: build-containers

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: unified_scraper
          POSTGRES_USER: scraper
          POSTGRES_PASSWORD: test_pass
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379

    steps:
    - uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'

    - name: Install dependencies
      run: |
        pip install -r backend/requirements.txt
        pip install pytest pytest-asyncio httpx

    - name: Create database schema
      env:
        PGPASSWORD: test_pass
      run: |
        psql -h localhost -U scraper -d unified_scraper < backend/db/schema.sql

    - name: Run integration tests
      env:
        DATABASE_URL: postgresql://scraper:test_pass@localhost:5432/unified_scraper
        REDIS_URL: redis://localhost:6379
      run: |
        pytest tests/integration/ -v
>>>>>>> iac032/main

  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

<<<<<<< HEAD
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Cargo audit
      working-directory: src-tauri
      run: |
        cargo install cargo-audit
        cargo audit

    - name: NPM audit
      run: npm audit --audit-level=high || true  # Allow moderate vulnerabilities for now
=======
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install safety
      run: pip install safety pip-audit

    - name: Check for vulnerabilities (pip-audit)
      run: pip-audit -r backend/requirements.txt || true

    - name: Check for secrets
      run: |
        pip install detect-secrets
        detect-secrets scan --baseline .secrets.baseline || true

  all-checks-pass:
    name: All Checks Pass
    runs-on: ubuntu-latest
    needs: [python-lint, python-test, build-containers, integration-tests, security-audit]
    if: always()

    steps:
    - name: Verify all jobs succeeded
      run: |
        if [ "${{ needs.python-lint.result }}" != "success" ]; then
          echo "❌ Python lint failed"
          exit 1
        fi
        if [ "${{ needs.python-test.result }}" != "success" ]; then
          echo "❌ Python tests failed"
          exit 1
        fi
        if [ "${{ needs.build-containers.result }}" != "success" ]; then
          echo "❌ Container builds failed"
          exit 1
        fi
        if [ "${{ needs.integration-tests.result }}" != "success" ]; then
          echo "❌ Integration tests failed"
          exit 1
        fi
        echo "✅ All checks passed! Ready to deploy."
>>>>>>> iac032/main
